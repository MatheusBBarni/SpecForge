@AGENTS.md

## Claude Code
- Use `src/AGENTS.md`, `src-tauri/AGENTS.md`, and `docs/AGENTS.md` as the path-scoped rules for this repo.
- Read `docs/agent-context/architecture.md`, `docs/agent-context/api-conventions.md`, and `docs/agent-context/testing-guide.md` on demand instead of duplicating them here.
- Update `HANDOFF.md` before `/clear` or compaction when the session gets dense.
